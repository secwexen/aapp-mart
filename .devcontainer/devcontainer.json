{
  "name": "AAPP-MART Dev Container",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
    "python": "3.11",
    "git": "latest",
    "docker-in-docker": "latest",
    "pipx": "latest",
    "curl": "latest",
    "wget": "latest",
    "poetry": "latest",
    "jupyter": "latest",
    "pytest": "latest"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "postCreateCommand": "pip install --upgrade pip && [ -f requirements.txt ] && pip install -r requirements.txt && [ -f requirements-dev.txt ] && pip install -r requirements-dev.txt",
  "remoteUser": "vscode"
}
